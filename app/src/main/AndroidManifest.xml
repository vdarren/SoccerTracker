<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.darrenwilliamson.st3">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartupActivity" android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginSuccessActivity" android:theme="@style/AppTheme.Dark"></activity>
        <activity android:name=".RegisterAccountActivity" android:theme="@style/AppTheme.Dark"></activity>
        <activity android:name=".AndroidDatabaseManager" android:theme="@style/AppTheme.Dark"></activity>
        <activity android:name=".LeagueTableActivity" android:theme="@style/AppTheme.Dark"></activity>
        <activity android:name=".MainActivity" android:theme="@style/AppTheme.Dark"></activity>
        <activity android:name=".MainHubActivity" android:theme="@style/AppTheme.Dark"></activity>
        <activity android:name=".ActivityFixtures" android:theme="@style/AppTheme.Dark"></activity>
    </application>

</manifest>